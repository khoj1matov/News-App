import 'package:flutter/material.dart';

class FavoriteCartView extends StatelessWidget {
  const FavoriteCartView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Favorite"),
      ),
    );
  }
}
